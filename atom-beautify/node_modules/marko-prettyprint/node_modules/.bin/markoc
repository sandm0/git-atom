#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../marko/bin/markoc" "$@"
  ret=$?
else 
  node  "$basedir/../marko/bin/markoc" "$@"
  ret=$?
fi
exit $ret
