#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../marko-prettyprint/bin/marko-prettyprint" "$@"
  ret=$?
else 
  node  "$basedir/../marko-prettyprint/bin/marko-prettyprint" "$@"
  ret=$?
fi
exit $ret
